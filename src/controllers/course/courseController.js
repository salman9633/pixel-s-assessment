const courseController = {
    addAnewCourse(req, res, next) {
        try {
            
            
        } catch (error) {
            return next(error)
        }
     }
}

export default courseController